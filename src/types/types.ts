export type TEventsCard = {
    _id: string,
    title: string,
    description: string,
    event_date: string,
    organizer: string
}

export type TParticipan = {
    _id: string,
    name: string,
    email: string
}